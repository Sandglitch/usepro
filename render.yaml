services:
  - type: web
    name: useless-route-generator
    env: python
    pythonVersion: 3.11.9
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: Maps_API_KEY
        sync: false
